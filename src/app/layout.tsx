import './globals.css';

export const metadata = {
  title: 'Iraq ERP',
  description: 'Iraq ERP — compliant with Iraqi Unified Accounting System and GCT',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
